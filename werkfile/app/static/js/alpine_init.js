// werkfile/werkfile/static/js/alpine_init.js
// Example: Initialize Alpine.js data or components if needed
document.addEventListener('alpine:init', () => {
  console.log('AlpineJS Initialized');
  // Alpine.data('dropdown', () => ({ open: false }))
})
